sources = files(
    'canvas.cpp', 'brush.cpp', 'brush_cursor.cpp', 'brush_body.cpp', 'eraser.cpp',
    'stroke_renderer.cpp', 'canvas_camera.cpp'
)

editor_lib = static_library('editor', sources,
    dependencies: global_deps
)